# En Render: agregar esta variable desde el panel de PostgreSQL (Internal Database URL)
# Ejemplo local: postgresql://usuario:password@localhost:5432/ventasapp
DATABASE_URL=postgresql://...

# Puerto (Render lo asigna automáticamente)
PORT=10000
